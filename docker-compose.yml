version: "3.9"

services:
  outboundai:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - outboundai_data:/data

volumes:
  outboundai_data:
