port: 7880
rtc:
  port_range_start: 50000
  port_range_end: 50200
  use_external_ip: true
  tcp_fallback_port: 7881

# Replace with your generated keys from: docker run --rm livekit/generate --keys
keys:
  REPLACE_WITH_API_KEY: REPLACE_WITH_API_SECRET

logging:
  level: info

redis:
  address: localhost:6379
